// Interface interface Shape {     void area();     void display(); }  // Circle Class class Circle implements Shape {     double radius;
    double result;     Circle(double radius) {         this.radius = radius;     }      public void area() {         result = Math.PI * radius * radius;     }      public void display() {         System.out.println("Circle Area: " + result);     } }  // Rectangle Class class Rectangle implements Shape {     double length, width;     double result;      Rectangle(double length, double width) {         this.length = length;         this.width = width;     }      public void area() {         result = length * width;     }      public void display() {         System.out.println("Rectangle Area: " + result);     } }  // Triangle Class class Triangle implements Shape {     double base, height;     double result;      Triangle(double base, double height) {         this.base = base;         this.height = height;     }      public void area() {         result = 0.5 * base * height;     }      public void display() {
        System.out.println("Triangle Area: " + result); }}  // Test Class public class RuntimeBinding {     public static void main(String[] args) {          // Runtime Binding using Shape reference         Shape s;          // Circle         s = new Circle(5);         s.area();         s.display();          System.out.println("----------------");          // Rectangle         s = new Rectangle(4, 6);         s.area();         s.display();          System.out.println("----------------");          // Triangle         s = new Triangle(3, 8);         s.area();         s.display();     } }
